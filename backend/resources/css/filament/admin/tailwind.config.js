// resources/css/filament/admin/tailwind.config.js

import colors from 'tailwindcss/colors'

export default {
    theme: {
        extend: {
            colors: {
                primary: colors.blue, // Esto hará que el color principal sea azul
            },
        },
    },
}
